// Seed script entrypoint used by `pnpm db:seed` and Playwright e2e setup.
// Add feature seed data here once the app has Drizzle tables under `src/features`.
// Keep seeds idempotent so repeated local/e2e runs can reuse the same database.
// Typical shape: import `db`, import feature tables, then insert/upsert demo rows.

console.info("No seed data configured yet.");
